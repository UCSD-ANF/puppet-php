Version ANF20120720-1

Usage:

include php # will install php command-line

include php::apache # will install mod_php and enable it using apache::module

# install a brief config snippet into /etc/php.d or similar called foo.ini
# with the contents given in the content parameter
php::config{'foo':
  content => ';this is a comment',
}
